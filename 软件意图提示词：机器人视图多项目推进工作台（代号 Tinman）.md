# 软件意图提示词：机器人视图多项目推进工作台（代号 Tinman）

## 0. 一句话意图
一个本地桌面工作台：把每个软件项目画成一个机器人，架构模块对应身体部位，
进度直接染在部位轮廓上；打开就能看出哪个项目的哪一块是短腿，
点一下短腿即可生成任务卡并派发给本地 agent CLI 执行。

目的不是记录进度，是**快速识别任务、快速派发任务**。

## 1. 目标与非目标

### 必须达成
- 添加本地项目文件夹后自动扫描仓库事实，无需手工填表
- 单项目视图：机器人部位轮廓即进度条
- 舰队视图：多项目机器人并列，按短腿严重度排序
- 从"看见短腿"到"任务已派发"不超过 3 次点击
- 全过程可观察、可手动接管（可见终端、文件树、Git 变更）

### 明确不做（防止跑偏）
- 不做甘特图、工时统计、人员排期等通用项目管理
- 不做代码托管、不做完整 IDE 嵌入
- 不自动 push / deploy / 删除 / 花钱
- 不用 AI 猜测进度百分比来填充画面

## 2. 概念模型

| 现实                 | 本软件               |
| -------------------- | -------------------- |
| 一个项目             | 一个机器人           |
| 架构层模块（6–7 个） | 机器人部位（slot）   |
| 部位内的子模块       | 部位内的线路（wire） |
| 模块的验收项         | 线路的通电状态       |
| 项目可交付           | 机器人激活、可落地   |

**两层严格对应两级责任人：**
- 部位层（架构）→ LLM 给推进建议，人可读
- 线路层（实现）→ 本地实施 agent 执行，机器可读

### 部位槽位（固定 7 个，不得扩展）
| slot      | 默认承载                     |
| --------- | ---------------------------- |
| head      | 决策 / 算法 / AI 层          |
| torso     | 核心域逻辑与数据模型         |
| left_arm  | 用户界面 / 前端              |
| right_arm | 对外接口 / 集成              |
| left_leg  | 基础设施 / CI                |
| right_leg | 部署 / 发布                  |
| backpack  | 可选扩展（观测、插件、实验） |

模块数超过 7 个时合并同类，不新增部位——部位越多，"一眼可读"的优势越弱。
项目未映射的槽位渲染为暗色轮廓，表示"本项目无此模块"，不是 0%。

## 3. 数据模型

本地 SQLite，四张核心表：

```
workspace(id, name, root_path, created_at, llm_profile_id, prefs_json)
part(id, workspace_id, slot, label, weight, planned_start, status)
wire(id, part_id, label, criteria_json, progress, status, evidence_json, updated_at)
task(id, workspace_id, part_id, wire_id, goal_json, state, station, worktree_path,
     dispatched_at, finished_at, result_json)
```

- `status` 允许值：`unmapped | pending | in_progress | done | blocked`
- `criteria_json`：该线路的验收项数组，每项 `{ text, met: bool, evidence }`
- `progress`（线路级）= 已满足验收项 / 全部验收项，取整
- 部位进度 = 其线路 progress 的加权平均；无线路的部位显示为 `pending`
- 项目进度 = 部位进度按 weight 加权平均，仅作卡片角落小号辅助信息

**禁止**由模型直接写入 progress 数值。progress 只能由验收项状态推导。

## 4. 仓库事实扫描

新建 workspace 时选择本地目录，只读扫描，产出 `facts.json`。

### 可扫的事实（必须实现）
- 目录结构与文件数、代码行数按语言分布
- 依赖清单（package.json / requirements.txt / Cargo.toml / go.mod 等）
- 入口与启动脚本（scripts、Makefile、Dockerfile、README 中的运行命令）
- 测试文件清单；能安全执行时记录通过/失败数，否则只记存在性
- 构建能否成功（可选，需用户开启）
- Git：最近提交时间、近 30 天活跃文件 Top N、分支、未提交变更
- TODO / FIXME / HACK 标记及所在路径
- 已有规格文档、设计稿、参考图路径
- 路由 / API 端点 / 页面清单（按框架约定探测，探测不到就留空）

### 明确扫不出的（不得伪造）
- 任何模块的"完成百分比"
- 用户的意图是否已被满足
- 代码质量好坏

### 扫描后的一次性引导（关键步骤）
LLM 读取 `facts.json`，提议：
1. 模块 → 部位的映射（含理由，每条一句话）
2. 每个部位下的线路清单
3. 每条线路的验收项草案（3–6 条，可观察、可验证）

以**单页确认表**呈现给用户，支持逐条修改和合并部位，确认后写库。
此后仓库变化只做增量更新，不重复完整引导。
未确认前，机器人以全暗轮廓 + "待确认架构地图" 提示显示，不显示虚假进度。

## 5. 进度与短腿计算

```
未完成度 u = 1 - progress/100

阻塞系数 b:
  blocked                        → 1.5
  停滞（>30 天无相关提交且未完成） → 1.2
  其他                           → 1.0

短腿分数 S = weight × u × b
```

- 舰队视图按各项目 max(S) 降序排列
- 部位层按 S 降序标记，S 最高者加"短腿"角标
- 额外区分两种状态，文案不同：
  - `planned_start` 尚未到达 → "按计划未开工"（不算短腿，不进排行）
  - 已开工但 progress 低于同项目部位中位数 20 个百分点以上 → "落后于其他部位"

短腿排行只取前 5 条，超出折叠。看得完才有用。

## 6. UI 框架（严格参照提供的截图，五区布局）

### 顶部
原生菜单栏：File / Edit / View / Help。窗口标题为当前 workspace 名。

### 左侧栏（宽约 260px，可折叠）
- 上部固定动作：`New Project`、`Search`、`Automations`、`Customize`
- 分组标题 `Projects`，右侧带筛选图标与"添加本地文件夹"图标
- 项目树：项目名（带图标）下挂最近任务条目，右侧显示相对时间（`7m` / `21h` / `1d`）
- 当前选中项高亮为浅灰色块
- 底部：账户卡片 + `Update` 主按钮 + 设置齿轮

### 中央区（主内容，三种视图切换）
- 顶部标题栏：项目名 + 视图切换（`Robot` / `Fleet` / `Task`）+ 右上 `IDE ↗`、`⋯`、面板开关
- 主体：见第 7 节
- 底部输入条（照图复刻）：
  - 左侧 `+` 附件按钮（支持拖入截图、参考图）
  - 占位文字：`描述你想做的功能，或直接问哪块该先动`
  - 右侧 LLM 选择器（对应图中 `Extra High Fast` 位置）
  - 语音输入按钮
  - 最底一行执行目标选择器（对应图中 `This PC`）：`This PC` / 具体 agent CLI / 指定工位

### 右侧动作栏（宽约 200px）
标题为当前上下文，如 `On left_leg · 基础设施`。动作项照图排列：
`Changes`（Git 变更） / `Browser`（预览） / `Terminal`（可见终端） / `Files`（文件树） / `Facts`（扫描事实）

这一栏是"可手动接管"的入口，任何时候都可点开真实终端，不得隐藏或延迟加载。

### 视觉基调
深色为默认，浅色通过一个 `prefers-color-scheme` 媒体查询支持。
所有颜色走 CSS 变量。

## 7. 视图规格

### 7.1 Robot 视图（单项目，默认）
- 内联 SVG 单人形机器人，几何简洁风格（圆角矩形 + 圆形），禁止写实插画
- 每个部位是独立填充容器：进度色**从部位底部向上填充**，填充高度 = 部位进度
- 部位描边常显，未填充部分半透明，0% 时轮廓仍完整可读
- 状态配色（CSS 变量）：
  - `unmapped` 暗色仅轮廓
  - `pending` 中性灰
  - `in_progress` 主题色
  - `done` 完成色
  - `blocked` 警示红 + 2s 周期缓慢脉冲（唯一常显动画）
- 短腿部位加一个小角标，不用额外动画抢注意力
- 悬停部位：tooltip 显示 部位名 / 进度 / 状态 / 短腿分数
- 部位内部绘制**线路（wire）**：细线条从部位中心向外延伸，已完成线路点亮，未完成线路暗色虚线。线路数 > 6 时只画 6 条并标注 `+N`
- 全部部位 100%：双眼点亮、一次性激活动画（轻微浮起 + 底部光晕扩散，≤1.5s，不循环）、角落出现 `READY` 徽标。克制，不浮夸

### 7.2 Fleet 视图（多项目并行）
- 自适应网格（`grid, auto-fill`，最小卡宽 240px），每项目一张卡
- 卡内为缩略机器人（不画线路）+ 项目名 + 项目总进度小号数字
- 卡片默认按 max(S) 降序，可切换为按名称 / 按最近活动
- 视图顶部一条**最短腿排行**：`项目名 · 部位名 · 进度 · 状态`，最多 5 行，点击直达该部位
- 卡片上有 blocked 部位时，卡片边框同步脉冲

### 7.3 部位钻取（点击部位）
右侧或下方展开面板，固定四段：
1. **事实**：该部位涉及的文件区域、测试状态、最近提交时间、TODO 数
2. **验收项**：逐条列出，已满足/未满足，附证据链接
3. **LLM 框架建议**：见 8.2
4. **线路清单**：每条线路一行，`状态 · 标签 · [生成任务]` 按钮

### 7.4 Task 视图（工位）
```
工位 1｜left_leg / CI 流水线    正在实现   [打开终端] [查看变更]
工位 2｜left_arm / 设置页       正在检查   [打开终端] [打开预览]
工位 3｜right_leg / 部署脚本    等待派发   [查看任务卡]
```
- 工位数可配置，默认 2；任务多于工位自动排队
- 空出工位自动领取下一个"可开工"任务
- 单个任务失败或额度不足，只暂停受影响任务，其余继续
- 每个任务完成后固定展示：`打开试用｜做成了什么｜检查了什么｜还有什么没做好`

## 8. AI 原生架构

### 8.1 LLM 接入
- Provider 可配置：OpenAI 兼容 endpoint + API key，支持本地 Ollama
- 每个 workspace 可绑定不同 profile；输入条的选择器切换本次调用所用模型
- 全部调用走同一层 `llm.call(purpose, input) → structured output`，purpose 枚举固定：
  `map_architecture` / `advise_part` / `draft_goal` / `verify_delivery`
- 所有调用的输入输出落库，可回看。密钥存系统钥匙串，不写入项目目录

### 8.2 LLM 的职责边界（部位层，仅此一层）

**允许输出**（结构化，每次调用固定这五项）：
1. `diagnosis`：这块为什么算短腿，引用具体事实（文件、测试、提交时间），一句话
2. `next_step`：框架层面先动哪一块，一句话
3. `entry_point`：从哪里接入（目录 / 模块 / 接口），不写具体代码
4. `shared_risk`：会碰到的公共部分，以及建议由哪个任务负责
5. `done_criteria`：什么算做好，3–5 条可验证条目

**禁止输出**：
- 具体代码、具体函数实现、逐文件 diff
- 进度百分比
- 无事实依据的推测（无依据时必须写 `事实不足：需要扫描 X`）

### 8.3 本地实施 agent 的职责（线路层）
接收 goal 卡，在独立 worktree 中完成实现、自测、回归，返回交付说明与证据。
软件本身不复制这套协议，只负责派发、记录、接续、验证。

分工一句话写进代码注释与设置页：
> Skill 决定：做什么、怎样拆、什么算做好。
> RIG 决定：交给谁、何时开工、中断后如何接续。
> 机器人视图让你看见：现在在做什么、还差哪一块，并且随时能接管。

## 9. 任务派发

### 9.1 goal 卡契约（软件与 agent 之间唯一的交接物）
```json
{
  "task_id": "t_0142",
  "project": { "name": "示例项目", "root": "/Users/x/code/demo" },
  "target": { "slot": "left_leg", "part": "基础设施", "wire": "CI 流水线" },
  "user_intent_verbatim": ["提交后自动跑测试，失败要告诉我哪一条"],
  "attachments": ["/refs/ci-screenshot.png"],
  "facts": { "test_runner": "vitest", "ci_config": null, "last_commit": "2026-08-12" },
  "framework_advice": {
    "next_step": "先补最小 CI 配置，只跑单测",
    "entry_point": ".github/workflows/",
    "shared_risk": "与 right_leg 部署脚本共用 node 版本，由本任务确定"
  },
  "done_criteria": [
    "推送后自动触发测试",
    "失败时输出失败用例名",
    "本地一条命令可复现"
  ],
  "assumptions": [
    "ASSUMED: 只跑单测不跑 e2e — 因为仓库无 e2e 用例。反转方式：追加 e2e 任务"
  ],
  "workspace": { "worktree_path": "/tmp/rig/t_0142", "branch": "rig/t_0142" },
  "delivery_format": "变更说明 + 自测结果 + 未完成项",
  "authorization": { "allow_push": false, "allow_deploy": false, "allow_spend": false }
}
```

### 9.2 派发规则
- 每个 agent 在**独立 git worktree** 中施工，禁止多个 agent 写同一工作目录
- 公共部分只指派一个任务负责，其余任务按约定接入
- 无依赖的任务可同时开工，有依赖的排队；不为追求全并行强行拆分
- 支持直接命令行调用的 CLI 由程序启动并传入 goal 卡；仅支持交互的 CLI 接入可见终端
- 任务状态以**实际交付与验证结果**判定，不以 agent 打印"完成"判定
- 任务返回后：自动核对 `done_criteria`，逐条标记，写回对应 wire 的验收项 → 机器人部位进度随之上升

### 9.3 中断与接管
状态全在库里，不在终端对话里。关掉软件再打开，工位状态与队列完整恢复。
任何任务可暂停、可打开终端手动接管、可放弃并保留 worktree。

## 10. 技术约束
- 桌面应用，需读本地文件夹与启动子进程：Tauri（推荐，体积小）或 Electron，二者择一并在 README 说明理由
- 前端：React + TypeScript，机器人图形为**内联 SVG**，不引入图表库
- 状态：本地 SQLite，单一 schema 迁移文件
- 仓库扫描**严格只读**；一切写操作只发生在 worktree 副本
- 无网络也能运行核心功能（扫描、可视化、看板）；LLM 不可用时降级为"事实 + 手填验收项"，不阻塞界面
- 机器人图形与配色全部由数据和 CSS 变量驱动，换项目不改代码
- 单项目扫描（10 万行以内仓库）在 10 秒内完成，扫描期间界面不冻结

## 11. 安全边界
- 默认禁止 push、deploy、删除、任何付费操作；需在 goal 卡中显式授权
- 密钥不落项目目录、不进日志
- 首次访问某个本地目录需用户显式选择，不做全盘扫描
- 破坏性操作前必须弹确认，且给出撤回方式

## 12. 验收标准
1. 添加一个真实本地仓库后，10 秒内出现事实摘要和架构地图确认表；确认后机器人渲染出各部位进度
2. 未确认架构地图时，机器人全暗并提示待确认，**不显示任何编造进度**
3. Fleet 视图放入 5 个项目，用户在 15 秒内能说出"哪个项目的哪一块最短"——找 3 个人实测，至少 2 人成功
4. 从 Fleet 视图的短腿排行到任务派发完成，点击次数 ≤ 3
5. 点击任一部位，LLM 建议五项齐全，且 `diagnosis` 中至少引用一条可点开的真实事实
6. LLM 输出中出现具体代码或进度百分比时，被校验层拦截并记录
7. 两个任务同时派发，各自在独立 worktree，互不覆盖；合并后整体检查有独立记录
8. 任务返回后，对应部位填充高度按已满足验收项比例上升，数值可追溯到具体证据
9. blocked 部位在单项目与 Fleet 两个视图同步脉冲
10. 强制退出并重启，工位状态、队列、任务历史完整恢复
11. 断网启动：扫描与可视化正常，LLM 区域降级提示，界面无报错
12. 窗口缩至 1024px 宽，三栏布局降级为两栏，机器人不变形

## 13. 实施顺序
1. **只读观察版**：本地目录接入 + 事实扫描 + `facts.json`（先不接 LLM）
2. 架构地图确认表（可手填）+ SQLite 落库
3. Robot 视图 SVG：部位轮廓填充 + 状态配色 + tooltip
4. Fleet 视图 + 短腿分数排序 + 排行条
5. 接入 LLM：`map_architecture` 与 `advise_part` 两个 purpose，含输出校验层
6. goal 卡生成 + 单工位派发 + worktree 隔离 + 可见终端
7. 多工位队列、依赖等待、失败隔离、交付核对回写进度
8. 完成态激活动画、响应式降级、按第 12 节逐条自测

每一步结束都必须是可运行、可给人看的状态。第 1–4 步做完，软件已经有用：能看、能比、能定优先级，只是还要手动派发。